from setuptools import setup, find_packages

setup(
    name='pythonProjectTest',
    version='0.1.0',  # Update with your version
    packages=find_packages(),
    install_requires=[
        'astropy',
        # ... other dependencies
    ]
)
